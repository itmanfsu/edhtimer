{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      ".indexOn": ["expiresAt"],
      "$room": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
